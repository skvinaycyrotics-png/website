import { ReactNode } from 'react';

export const metadata = {
  title: 'Business Sectors | CYROTICS TECHNOLOGIES',
  description: 'Delivering specialized technology solutions for Urban Development, Public Sector, Telecom, Logistics, Healthcare, Education, Energy, and Mobility.',
};

export default function BusinessSectorLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
